import 'package:flutter/material.dart';

// Stubs para las rutas solicitadas. Reemplaza el contenido por tus pantallas reales.

class Principal extends StatelessWidget {
  const Principal({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Principal')), body: const Center(child: Text('Principal')));
  }
}

class Productos extends StatelessWidget {
  const Productos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Productos')), body: const Center(child: Text('Productos')));
  }
}

class ProductosOficina extends StatelessWidget {
  const ProductosOficina({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Productos Oficina')), body: const Center(child: Text('Productos Oficina')));
  }
}

class ProductosMedicos extends StatelessWidget {
  const ProductosMedicos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Productos Médicos')), body: const Center(child: Text('Productos Médicos')));
  }
}

class ProductosTransporte extends StatelessWidget {
  const ProductosTransporte({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Productos Transporte')), body: const Center(child: Text('Productos Transporte')));
  }
}

class ProductosTecnologicos extends StatelessWidget {
  const ProductosTecnologicos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Productos Tecnológicos')), body: const Center(child: Text('Productos Tecnológicos')));
  }
}

class Categorias extends StatelessWidget {
  const Categorias({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Categorias')), body: const Center(child: Text('Categorias')));
  }
}

class Productos2 extends StatelessWidget {
  const Productos2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Productos 2')), body: const Center(child: Text('Productos 2')));
  }
}
