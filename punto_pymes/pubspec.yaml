name: punto_pymes
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Íconos estilo iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 🔹 Aquí se declaran los recursos (imágenes)
  assets:
  - assets/images/image.png
  - assets/images/per.png
    # O puedes usar la línea de abajo si quieres cargar toda la carpeta:
    # - assets/images/
