name: punto_pymes
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Íconos estilo iOS
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.3
  image_picker: ^0.8.9
  crypto: ^3.0.2
  geolocator: ^9.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 🔹 Aquí se declaran los recursos (imágenes)
  assets:
    - assets/images/
